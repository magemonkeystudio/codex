name: Dev Build

on:
  push:
    branches: [ dev ]
    paths: [ '**/src/**' ]

jobs:
  deploy:
    uses: magemonkeystudio/.github/.github/workflows/reusable-devbuild.yml@main
    secrets: inherit
    with:
      maven_args: '-f pom-dev.xml'
      artifact_filter: 'codex'
