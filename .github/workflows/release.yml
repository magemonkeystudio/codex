name: Release Build

on:
  push:
    branches: [ main ]
    paths: [ '**/src/**' ]

jobs:
  deploy:
    uses: magemonkeystudio/.github/.github/workflows/reusable-release.yml@main
    secrets: inherit
    with:
      build_spigot: true
      artifact_filter: 'codex'
